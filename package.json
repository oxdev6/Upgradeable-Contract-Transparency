{
  "name": "proxyscope-w1d1",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @proxyscope/core && npm run build -w @proxyscope/cli && npm run build -w @upgradewatch/core",
    "test": "npm run test -w @proxyscope/core && npm run test -w @upgradewatch/core",
    "test:run": "npm run test:run -w @proxyscope/core && npm run test:run -w @upgradewatch/core",
    "dataset:upgrade-history": "npm run generate-dataset -w @upgradewatch/core"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.9.3"
  }
}
