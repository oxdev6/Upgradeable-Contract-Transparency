{
  "name": "@upgradewatch/core",
  "version": "0.1.0",
  "description": "Upgrade event parsing for EVM proxy contracts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest",
    "test:run": "vitest --run",
    "generate-dataset": "npm run build && node ./dist/dataset/runDataset.js"
  },
  "dependencies": {
    "viem": "^2.46.2"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
